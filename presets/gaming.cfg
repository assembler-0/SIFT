# SIFT Gaming Performance Preset
# Tests that matter for gaming performance

# Test execution order (comma-separated)
test_order = cache, branch, avx, mem

# Cache hierarchy tests (important for gaming)
cache_iterations = 5000

# Branch prediction (AI, physics, game logic)
branch_iterations = 1000000
branch_pattern = 4

# AVX performance (SIMD operations)
avx_iterations = 50000
avx_lower = 0.0001
avx_upper = 1000000

# Memory performance (texture streaming, asset loading)
mem_iterations = 10